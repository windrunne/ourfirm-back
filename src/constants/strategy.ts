export const VISION = {
  sendWidth: 1000,
  jpegQuality: 80,
  ocrEngine: "2",
} as const;

export const LLM = {
  sendWidth: 1100,
  jpegQuality: 82,
} as const;

export const OCR_MIN_TEXT_LENGTH = 12;
